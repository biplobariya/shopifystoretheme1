<!doctype html>
<html lang="{{ request.locale.iso_code }}">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>{{ page_title }}{% if current_tags %} – {{ current_tags | join: ", " }}{% endif %}{% if current_page != 1 %} – Page {{ current_page }}{% endif %} – {{ shop.name }}</title>

      {{'tailwind-output.css' |  asset_url |  stylesheet_tag}}
      {% comment %} {{'theme.css' |  asset_url |  stylesheet_tag}} {% endcomment %}

    {{ content_for_header }}
  </head>

  <body>

    {% comment  %} Header Group {% endcomment %}
    {% sections 'header-group' %}


    <main id="MainContent" class="container mx-auto">{{ content_for_layout }}</main>

    {% comment %}Footer Group {% endcomment %}
    {% sections 'footer-group' %}
    


  </body>
</html>
